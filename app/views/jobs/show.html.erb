<!-- 	#  title        :text
		#  location     :text
		#  salary       :float
		#  industry     :text
		#  description  :text
		#  closing_date :date
		#  created_at   :datetime         not null
 -->

<div class="a-form">

	<p>Title : <b><%= @job.title %></b> </p>

	<p>Employer : <b><%= link_to @job.employer.name, @job.employer %></b> </p>

	<p>Location : <b><%= @job.location %> </b></p>

	<p>Salary $: <b><%= number_with_precision(@job.salary , :precision => 2) %> </b></p>

	<p>Industry : <b><%= @job.industry %> </b></p>

	<p>Closing Date : <b><%= @job.closing_date.strftime("%d-%b-%Y") %> </b></p>

	<p>Description : <b><%= @job.description %> </b></p>

	<p>Created On : <b><%= @job.created_at.strftime("%d-%b-%Y %H:%M %p") %></b></p>

	<p>So far <b><%= @job.appls.count %> applications have been lodged.</b></p>

	<% if @current_employer.present? %>
		<div>
			<%= link_to 'Delete', @job, :method => 'delete', :data => { :confirm => 'Are you sure?'} %>
			<%= link_to 'Edit', edit_job_path(@job) %>
		</div>
	<% else %>
		<p class="greysmall">Login as Employer to Delete/Edit Job Advertisement</p>
	<% end %>

	<% if @current_jobseeker.present? %>
			<% session[:job_id] = @job.id %>
			<%= link_to 'Apply', new_appl_path %>
	<% else %>
		<p class="greysmall">Login as Jobseeker to Apply for this Job</p>
	<% end %>

</div>




