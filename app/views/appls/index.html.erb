<!-- 	#  resume       :text
		#  cover_letter :text -->
<h2>Applications for <%= @name %></h2>

<ul>
	<% @appls.each do |appl| %>
		<li>
			<ul>
				<%= link_to(appl.job.title, appl) %>
				Applied : <%= appl.created_at.strftime("%d-%b-%Y %H:%M %p") %>
			</ul>
		</li>
	<% end %>
</ul>